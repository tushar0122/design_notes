WebSocket

Uses 
    => WebSockets are great for real-time, event-driven communication

difference with restapi in http   
    => Scaling 
        => REST APIs can be easily scaled using load balancers since each request is independent.
        => WebSocket connections require sticky sessions (clients need to stay connected to the same server) or a pub-sub mechanism to scale across multiple instances
    => 